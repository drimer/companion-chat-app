# Copy this file to settings.env and fill in environment-specific values.
# Lines starting with # are comments and ignored by flutter's define loader.
API_BASE_URL=http://localhost:4000

# Cognito SSO configuration (update per environment)
COGNITO_ISSUER=
COGNITO_USER_POOL_ID=
COGNITO_CLIENT_ID=
COGNITO_REGION=
COGNITO_REDIRECT_URI=
COGNITO_SCOPES=